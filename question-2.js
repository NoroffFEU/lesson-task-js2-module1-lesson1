// Question 2:
const user = {
  firstName: 'Ola',
  lastName: 'Nordmann',
  currentModule: 'JavaScript 2',
};

const { firstName, lastName, currentModule } = user;

const greeting = `My name is ${firstName} ${lastName} and I am on ${currentModule}.`;
